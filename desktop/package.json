{
  "name": "nebula-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "main.mjs",
  "scripts": {
    "start": "electron .",
    "start:dev": "VITE_DEV_SERVER_URL=http://localhost:5173 electron .",
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "test:smoke": "NEBULA_DESKTOP_SMOKE=1 electron ."
  },
  "devDependencies": {
    "electron": "44.3.0",
    "ws": "^8.18.0"
  }
}
