# Website Assets

Approved public images for the portfolio sync live here.

Expected filenames:

- `hero.png` - primary project image.
- `thumbnail.png` - compact project card image.
- `gallery-01.png`, `gallery-02.png`, etc. - optional supporting images.

Only place images here if they are safe to publish on `justinperea.com`.
