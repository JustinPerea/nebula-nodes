export function EditorView() {
  return (
    <div style={{ position: 'fixed', inset: 0, color: '#fff', padding: 80, background: '#000', zIndex: 50 }}>
      Editor placeholder — full surface lands in Task 16
    </div>
  );
}
